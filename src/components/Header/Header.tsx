import React from 'react';
import { StyledArrow, StyledHeader } from './styled';

function Header() {
  return (
    <StyledHeader>
      <StyledArrow />
      <h3>Let’s plan your saving goal</h3>
    </StyledHeader>
  );
}

export default Header;
